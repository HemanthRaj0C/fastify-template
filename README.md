# Fastify Backend Template

A minimal Fastify backend template.

## Quick Start

```bash
npm install
npm run dev
```

## Available Routes

- `GET /api/health` - Health check endpoint

## Environment Variables

Copy `.env.example` to `.env` and configure as needed.
